// app.route.ts
import { Routes } from '@angular/router';
import { Home } from './pages/home/home';  // ajuste o caminho se necessário



export const routes: Routes = [
  { path: '', component: Home }, // rota raiz
];
